Figma URL을 받아 배포 가능한 HTML/CSS/JS + assets를 생성합니다.

사용자 입력: $ARGUMENTS

---

이 명령은 `figma-to-code` 스킬을 실행합니다. 반드시 Skill 도구로 `figma-to-code` 스킬을 호출하고, 스킬의 모든 규칙을 따르세요.

사용자 입력(`$ARGUMENTS`)을 그대로 스킬에 전달하세요.
